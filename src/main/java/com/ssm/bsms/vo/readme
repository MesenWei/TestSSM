vo存在的意义
1.有时候前台显示的数据是多个po中的字段，为了减少向前台传递数据的流量，将多个对象转换为前台所需属性的一个对象。
2.有时候数据库中存的是一些id，但前台需要展示的是id对应的一些属性，所以不得不在对象中新增属性来存储一些id对应的name的值，
    但存储在po中，会降低系统的灵活性，所以将那些字段存在vo中比较妥当。
    在方法返回的时候，po和vo要分开使用，返回值类型的统一性是无所谓的，po转vo需要耗费一部分性能，所以不要都用vo。